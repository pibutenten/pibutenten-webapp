/**
 * 시술 리포트 표시 문구 SSOT (작업 B, 2026-06-04).
 *
 * 명칭 2단 규칙:
 *   - **서술형** = '경험' — 문장 안에서 모집단을 가리킬 때. (예: "N건의 경험을 모았어요")
 *   - **목록 라벨** = '후기' — 개별 후기 목록 제목·버튼. (예: "후기 N개")
 *
 * 코드·DB·라우트·영문 review 식별자는 불변 — 표시 문자열만.
 * SEO 메타데이터(/reports title·desc·OG)·RSS 는 검색 의도상 '후기' 유지(여기 헬퍼 미적용).
 */

/** 서술형 경험 카운트 — "N건의 경험". */
export function experienceCount(n: number): string {
  return `${n}건의 경험`;
}
