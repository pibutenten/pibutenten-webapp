/**
 * /admin 하위 페이지 좌상단 백 링크.
 *
 * Deprecated (2026-05-15):
 *   모든 /admin/* 페이지가 이미 자체 BackButton 컴포넌트를 갖고 있어 중복 표시됨.
 *   사용자 요청에 따라 이 옛 링크는 비활성 (null 반환).
 *   향후 layout 에서 import 제거 시 이 파일도 함께 삭제 예정.
 */
export default function AdminBackLink() {
  return null;
}
