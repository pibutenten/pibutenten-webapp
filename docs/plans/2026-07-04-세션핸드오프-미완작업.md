# 세션 핸드오프 (2026-07-04) — 완료 요약 + 미완 파이프라인

> 다음 세션이 이 문서만 읽고 이어갈 수 있게 작성. 완료되면 이 문서를 삭제하고 CHANGELOG 에 기록.

## 1. 이번 세션에서 배포 완료된 것 (전부 검수·실측 검증·push 완료)

| 커밋 | 내용 |
|---|---|
| 7184ad1 | 피드 카테고리 탭 서버 풀(/?cat= URL SSOT, 마이그 0326) + 댓글수 신선도(PTR ref 초기화) + 투데이 KPI 링크 |
| 21724bb | 전체 피드 Q&A 슬롯 보장(20장당 ≥6, blendQaQuota) + 의사글 x3(마이그 0327) + 댓글 "N개 모두 보기" 복원 |
| d48f521 | 관리자 KPI deleted_at 필터 정합(검수대기 6→3 등) + 리포트 조회수 기록 복원(ReportViewTracker 재배선) |
| f3015c2 | /admin/review-reports 확장(0328: 생성일·만족도분포·다운타임·효과 top3) + 헤더 정렬 + 대시보드 직행 |
| 3736aa3 | 표 정돈(전 칸 중앙정렬·분포바→툴팁·그룹 제거·기본 후기수 내림차순) |

적용된 마이그레이션: 0326·0327·0328 (모두 production 적용·실측 검증·DATABASE.md 기재 완료).

## 2. ⚠ 미완 파이프라인 — 작업 트리에 구현 완료·검수 대기 상태로 남아 있음

**원장 승인 완료**(2026-07-04: "표기안 승인 / 제목 9건 정정 승인 / 덴서티 알파팁 사전등록 / 리쥬란아이는 사전 기준")이며 서브에이전트 구현까지 끝났고, **검수→적용→커밋만 남음**. 작업 트리 파일:

- `supabase/migrations/0329_report_anchor_data_fixes.sql` (신규) — 데이터 정정 3건: ① 앵커 제목 인코딩 오염 9건(id 2925·2689·2959·3786·2966·3810·2970·3046·3056) '피부텐텐 리포트 | {ko}' 재적용 ② 리쥬란아이 앵커 post_slug rejuran-eye→rejuran-i(사전 SSOT 기준) ③ tag_dictionary 에 덴서티 알파팁(en=density-alpha-tip) 등록
- `supabase/migrations/0330_procedure_review_downtime.sql` (신규) — feed_cards_scored·search_cards_scored 의 procedure_review jsonb 에 downtime 추가(시그니처 불변 OR REPLACE)
- `src/components/card/ReviewSummary.tsx` — 재시술·다운타임 세그먼트 추가 + 효과 3개+"+n"
- `src/components/skin/ui.tsx` — PostCard(review 카드) 제목 아래 ReviewSummary 렌더
- `src/lib/card-select.ts` — CARD_LIST_SELECT 두 곳에 downtime 추가
- `src/lib/types/card.ts` — ReviewSummaryData 타입 확장

구현 에이전트 상세 보고는 워크플로 산출물 참조:
`C:\Users\Bae\.claude\projects\D--Dropbox-Claude-Code-260503----------\93406477-87cd-4fcb-bf51-0be89d2696c0\subagents\workflows\wf_c8b6393d-7c8\`

### 다음 세션 절차 (디렉터 프로세스 그대로)
1. diff 직접 검사 → 코드검수관 2렌즈 + 디비전문가(0329·0330) 교차 검수 → 치명 반영
2. 마이그 적용 — **둘 다 한국어 포함 → 반드시 `node scratchpad/db.mjs <파일>` UTF-8 경로** + U+FFFD 재스캔
   - 0329 적용 후 검증: 제목 오염 0건 / rejuran-i 매칭 / admin 표 61→63행
   - 0330 적용 후 검증: procedure_review 에 downtime 포함 실측
3. `npx tsc --noEmit` + `npm run build`
4. 브라우저 E2E: 피드 시술후기 카드에 요약 줄(★·통증·재시술·회복·효과 3+n) / admin 표 63행 / 리포트 제목 정상.
   ⚠ **로컬 검증 시 PWA 서비스워커 먼저 해제**(아래 §4 함정) — 안 하면 낡은 클라 코드로 오판
5. 커밋·push + CHANGELOG·DATABASE.md(0329·0330) 기재 + 이 문서 삭제

### 원장 확정 표기안 (구현 검수 시 대조용 SSOT)
- 형식: `★★★★☆ · 통증 {없음|조금|보통|꽤|심함} · {재시술} · {다운타임} · 효과 라벨·라벨·라벨 +n` (미응답 항목 생략)
- 재시술: yes→**또 받을래요** / maybe→**재시술 고민 중** / no→**재시술 생각 없어요**
- 다운타임: same_day→**당일 회복** / days_1_2→**회복 1~2일** / days_3_5→**회복 3~5일** / week_1→**회복 약 1주** / weeks_2_plus→**회복 2주 이상**
- 효과: 최대 3개 + 나머지 "+n"

## 3. 원장 확인 대기 (배포됨 — 실기기 확인만)
- 피드: Q&A/끄적끄적 탭 스크롤 · 전체 탭 20장당 Q&A 6장 · 댓글 "모두 보기" 줄 · 댓글수 PTR 갱신
- 투데이 KPI 타일 탭 이동(로그인 화면이라 자동검증 불가였음) · 온보딩 피부톤 선택(0a9df34)
- /admin/review-reports 새 표(중앙정렬·후기수순·툴팁 분포)

## 4. 백로그 (비차단)
- **PWA SW 가 dev 에서도 등록되어 낡은 클라 청크 서빙** — SW 등록 production 게이트 필요(이번 세션 실검증 중 발견, 디버깅 큰 함정)
- 관리자 잠재 불일치: doctor KPI 축(author OR doctor vs doctor 단독) / users limit 500 / doctorPickCount deleted 미필터 / reports 200건 상한 / dead component admin/tags/TagQueue.tsx
- 리포트↔태그사전 정합 자동 점검(이번 drift 2건 재발 방지) / 화면 전면 교체 시 "보이지 않는 부속(조회기록·점수)" 이식 점검 목록화
- 위생 라운드 잔여(env 문서·마이그 번호 문서화·미사용 export·capacitor 주석) / 색·디자인 토큰 라운드(P7) / 앱 실기기 확인 2건(로그아웃 게이트·시작하기) / Vercel CLI 재로그인
