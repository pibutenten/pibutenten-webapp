# 260709 리포트·마이페이지 디자인 보정 계획 (R5)

> 명세 SSOT: 원장 R5 피드백 33항목 (R4 결과물 — 커밋 bc56ccd — 에 대한 후속 피드백).
> 선행 트랙: `260709 리포트 디자인 정밀 보정 계획 (R4).md` (완료·배포). 본 R5 는 그 후속.
> 상태: **v1.1 (원장 확정 반영 — 구현 진행)** — §4 결정 항목 전부 확정값 기입(2026-07-09). 전 항목 현재값을 실제 파일에서 실측 인용함.

---

## 1. 범위 · 불변 조건 (회귀 금지)

| 화면 | 파일 |
|---|---|
| 리포트 1뎁스 | `src/components/report/ReportSummaryBox.tsx` · `src/app/reports/ReportsIndexCard.tsx` |
| 리포트 2뎁스 | `src/app/reports/[procedure]/ReportsDetailView.tsx` (+`src/components/icons/index.tsx`) |
| 마이페이지 | `src/components/skin/mypage/MyPageView.tsx` |
| 프로필(내 정보) | `src/components/skin/u/[handle]/ProfileView.tsx` |
| 셸 공용 | `src/components/skin/AppShell.tsx` + `src/components/skin/app.module.css` |

불변 조건:

1. **ReportSummaryBox 는 /topics 닫힌 글상자와 공유 SSOT** (`topics/[tag]/TopicTagView.tsx:91`) — 항목 2·3·3'·5 는 /topics 에도 그대로 반영(의도된 동반 변경). 완료 후 두 화면 모두 검증.
2. **버튼·링크 글자색은 반드시 인라인 style** — `app.module.css:144-154` 의 `:where(.root) a{color:inherit}` / `:where(.root) button{color:inherit}` 이 **무계층(unlayered)** 이라 Tailwind v4 `@layer utilities` 의 `text-*` 를 캐스케이드에서 이긴다(R4 검증에서 button 3곳 실증 — **anchor 도 동일 함정**). 항목 1·18 의 근본 원인.
3. R4 확정 사항 재변경 금지: 통증 4스톱 토큰 SSOT(--pain-grad-1~4)·마커 #F06258 고정·오버레이 채움 방식·Cinzel 사용 범위(히어로 %·순위 넘버링만)·저장/공유 바 인라인 슬롯 포털(훅 인스턴스 단일)·카피 엔진 문구.
4. `app.module.css` 공용 클래스(.header/.chipRow/.card 등)의 **전역 수정 금지** — 반드시 variant 스코프(`.canvasMy .header` 식) 또는 신규 modifier 클래스로만 추가. 비-variant 화면(피드·투데이·내노트·글쓰기·admin)에 무영향이어야 함.
5. `.topStackHidden` 의 `-62px`(app.module.css:236-242)는 헤더 높이 전제 — 헤더 행 높이 결정자(아이콘 38px)를 바꾸지 않는다(항목 20·31 의 타이틀은 19px 이하 → 높이 불변).
6. 접근성 유지: aria-expanded/aria-pressed/role, h1 계층(모바일 타이틀 헤더 승격 시 본문 h1 은 sr-only 로 유지 — §3 참조).
7. 렌더 검증 전 dev 서비스워커 해제(메모리 '낡은 청크 함정'). dev 서버 http://localhost:4000 (죽이지 말 것).

---

## 2. 항목별 표

### 2.0. 공통 — 캔버스 페이지 헤더 동색화 (공통 · 21 · 27 근본 원인 공유)

| # | 항목 | 현재 (파일:줄 · 실제값) | 변경 후 | 파급·주의 |
|---|---|---|---|---|
| 공통 | 리포트 1뎁스 헤더를 바디와 같은 #F5FBFF 로 | `app.module.css:209-227` — 모바일 `.header` 는 base `var(--tt-canvas-top)` 위에 **하드코딩 틴트** `background-image: linear-gradient(180deg, rgba(232,245,253,.92), rgba(232,245,253,.75))` + blur(12px). ≥900px(`:2006-2012`)은 `background-color:#eef7fc` 하드코딩. → canvas="report" 가 `--tt-canvas-top:#f5fbff` 로 재정의해도(:122-125) 틴트가 덮여 바디와 색이 다름 | variant 스코프 오버라이드 1규칙 신설(§3.1): `.canvasReport .header, .canvasMy .header, .canvasProfile .header { background-color: var(--tt-canvas-top); background-image: none; backdrop-filter: none; }` → report=#F5FBFF·my/profile 은 21·27 변경 후 #F5FBFF | specificity (0,2,0) > `.header`(0,1,0) 이라 모바일·데스크탑 블록 모두 이김. 비-variant 화면 무영향. 헤더 유리 질감 상실은 의도. **데스크탑(≥900) 헤더에도 적용할지 원장 확인**(기본안: 적용) |
| 21 | /my 배경(캔버스+헤더) #F5FBFF | `app.module.css:126-129` `.canvasMy { --tt-canvas-top:#daf1fb; --tt-canvas:#daf1fb }` | 두 값 모두 **#f5fbff** (헤더는 위 공통 규칙이 추종) | MyPageView 태그칩 bg `#DAF1FB`(MyPageView:95 — "페이지 배경과 동일 톤" 주석)가 이제 배경과 대비 생김 — **칩 색 변경 지시 없음 → 유지**, 주석만 갱신. sticky 요소·상태바 필러(--tt-canvas-top 참조)는 자동 추종 |
| 27 | /[handle] 배경(캔버스+헤더) #F5FBFF | `app.module.css:130-133` `.canvasProfile { … #eaf2f8 }` | 두 값 모두 **#f5fbff** | ProfileView EmptyCard 주석(:637 "캔버스(#EAF2F8)") 갱신. 3 variant 가 전부 #F5FBFF 로 동일해지지만 클래스 3개는 유지(향후 분화 대비) |

### 2.1. 리포트 1뎁스

| # | 항목 | 현재 (파일:줄 · 실제값) | 변경 후 | 파급·주의 |
|---|---|---|---|---|
| 1 | "리포트 더보기" 텍스트 #ffffff | `ReportsIndexCard.tsx:269-280` — Link 에 `text-white`(:273) 클래스. **무계층 `a{color:inherit}` (app.module.css:144-147)에 패배** → 실제 렌더는 상속색(≈--ink-900 #1E2A35) | className 의 `text-white` 를 **인라인 `style={{ color:"#fff" }}`** 로 이전(배경 theme.color 인라인은 현행). 좌측 "내 후기 남기기"(:262 `text-[#3A3C41]`)도 동일 함정 → 인라인 `color:"#3A3C41"` 동반 명시 | R4 §검증에서 button 만 고치고 anchor 를 놓친 것 — 주석에 anchor 도 함정임을 추가. 18번과 동일 원인 |
| 2 | 통증·만족도 텍스트+수치+아이콘 20% 확대 | `ReportSummaryBox.tsx` — 라벨 `text-[13px]`(:120,135) · 수치 `text-[22px]`(:126,140) · 아이콘 size 16(:125,139) · "응답 적음" `text-[10px]`(:131) | 라벨 **15.5px** · 수치 **26.5px** · 아이콘 **19** · "응답 적음" **12px** (×1.2 환산, 0.5px 단위) | /topics 동반 변경(불변 조건 1). 우측 블록 폭 증가 → 좌측 막대 flex-1 이 흡수(렌더 확인). gap-4(:118)·gap-1 유지 |
| 3 | 통증·만족도 블록 ↔ 재시술 막대 하단 라인 정렬 | `ReportSummaryBox.tsx:101` `mt-3 flex items-end gap-5` — 박스 하단은 이미 정렬이나, 수치(leading-none)의 폰트 디센트만큼 **잉크 하단이 막대보다 ~2px 위** | `items-end` 유지 + 우측 그룹(:118)에 **`translateY(2px)` 보정**(26.5px 확대 후 렌더 실측으로 ±1px 확정) | "응답 적음"(통증 무응답) 케이스도 렌더 확인. /topics 동반 |
| 3' | 재시술 % 큰 숫자 bold→SemiBold | `ReportSummaryBox.tsx:94` `text-[50px] font-bold` · % `text-[24px] font-bold`(:97) | 둘 다 **font-semibold(600)** (숫자+% 세트 일관) | /topics 동반. R4 A-4(bold 확정)의 원장 재변경 — 계획서 충돌 아님 |
| 4 | 통증 마커 안 번개 가운데 정렬 + 약간 확대 | `ReportsIndexCard.tsx:193-202` — 마커 원 20px(h-5 w-5) 안 `IconPain size={11}`. IconPain(icons/index.tsx:108-117) viewBox `0 0 17 22` — 대각 글리프라 **광학 중심이 기하 중심에서 미세하게 어긋남** | size **11→12** + 아이콘에 광학 보정 **`translate` ±0.5px 단위**(렌더 대조로 확정 — 예: `relative left-[0.5px]`) | 마커 20px 원 유지(원 크기는 지시 없음). 13번(2뎁스)과 반드시 동일 값 |
| 5 | 96% ↔ "재시술의향" 라벨 간격 반절 | `ReportSummaryBox.tsx:101` `mt-3`(12px). 단 큰 숫자 `leading-[0.9]`(:94)라 실제 잉크갭은 12px 초과 | **mt-3 → mt-1.5(6px)** 시작, 잉크갭 반절 기준 렌더 대조 확정 | /topics 동반. 3번(하단 정렬)과 같은 컨테이너 — 한 번에 렌더 검증 |
| 6 | 효과 마지막 막대 ↔ "효과는 1~2주 후부터…" 간격 1/3 축소 | `ReportsIndexCard.tsx:250` 효과시점 p `mt-8`(32px) | **mt-8 → mt-5(20px)** — "현재의 1/3 만큼 축소"(32×⅔≈21) 해석 | ⚠ 모호(§4): "1/3 로 축소"(≈11px, mt-3) 해석 가능 — 기본안 20px 렌더 후 원장 확인. B-18 의 개별 mt 체계 유지(다른 블록 32/20 불변) |

### 2.2. 리포트 2뎁스 (ReportsDetailView.tsx)

| # | 항목 | 현재 (파일:줄 · 실제값) | 변경 후 | 파급·주의 |
|---|---|---|---|---|
| 7 | 히어로 "피부텐텐 리포트" → TT symbol.svg | `:649` `<div class="text-[15px] font-bold tracking-[0.28em] text-white/55">피부텐텐 리포트</div>` | **자산 대기** — `전달용` 폴더 전수 확인 결과 미수령(§4). 수령 즉시: icons 모듈에 `IconBrandTTSymbol` 신설(잉크 bbox 크롭 — IconBrandTT 패턴 :639-645 참조), :649 텍스트 div 를 `<IconBrandTTSymbol fill="#FFFFFF(또는 white/55)" height={시안 실측}/>` + `<span class="sr-only">피부텐텐 리포트</span>` 로 교체. 높이는 현행 잉크(15px×1.3)에 맞춰 시작 후 시안 대조 | 아래 시술명 `mt-[14px]`(:651)은 잉크갭 재측정 필요. 워터마크 IconBrandTT(:641)와 별개 자산 |
| 8 | 만족도 분포 숫자 좌측정렬·간격 반절·바 확장 방향 | `:787` 행 `flex items-center gap-2`(라벨↔바↔숫자 모두 8px) · `:796` 숫자 `w-8 shrink-0 text-right` | `gap-2` 해체 → 라벨에 `mr-2`(8px 유지), 숫자 `text-right→text-left` + `ml-1`(4px=반절) · `w-8` 유지(숫자 시작 x 고정 = "위치 고정") · 채움 좌→우 확장(:791-794)은 현행이 이미 부합 | 5줄 전부 동일 행 구조라 한 곳 수정. 자릿수 달라도 숫자 좌변 정렬됨(의도) |
| 9 | 4.9 크기 ×0.68 | `:769` `text-[46px] font-semibold` | **46 → 31px** (46×0.68=31.3) | 좌측 블록 축소로 별 5개(:774)와의 세로 리듬 렌더 확인 |
| 10 | /5.0 확대+색 | `:772` `text-[15px] font-semibold text-[#7F838D]` | **17px / #B7C1C6** (semibold 유지) | 9번과 같은 `items-baseline` 행 — 함께 렌더 대조 |
| 11 | 통증 게이지 — 마커까지만 그라데이션 | `:815` `<div class="relative mt-5 h-[10px] rounded-[6px]" style={{background: PAIN_GRADIENT}}>` — **트랙 전체가 그라데이션(R4 오버레이 누락 확정)**. 1뎁스(IndexCard:180-191)·다운타임(:845-854)은 오버레이 적용됨 | 1뎁스 B-2 와 동일 오버레이: 내부에 `overflow-hidden rounded-[6px]` 그라데이션 풀폭 div + 우측 `span width:${100-painPos(avgPain)}%` `bg-[var(--gauge-track)]`(=#EDF2F4 — globals.css:112, 지시색과 일치), 마커 `z-[1]` | 정적 채움(애니메이션 없음 — 1뎁스와 동일). 마커(:816-827) 구조 불변 |
| 12 | 섹션 제목 어미 | `:807` "얼마나 아프고, 얼마나 쉬어야 할까?" | "얼마나 아프고, 얼마나 쉬어야 **할까요?**" | 문자열 1건 |
| 13 | 2뎁스 통증 마커·아이콘 = 1뎁스 4번과 동일 | `:826` `IconPain size={11}` (마커 20px/1px/#F06258/그림자 — 이미 1뎁스와 동일) | **size 12 + 4번과 동일한 광학 보정값** | 4번 확정값 그대로 복제(양쪽 상수 주석으로 상호 참조) |
| 14 | 타임라인 축선이 원을 가림 | `:967` 축선 `absolute … bg-[#E3EAF0]` 이 뒤에 오는 `:968` 원 grid(정적 배치)보다 **위에 페인트**(absolute > in-flow) | `:968` 원 컨테이너 `div.grid` 에 **`relative` 추가** → DOM 후순위 positioned 가 승리, 원(bg-white)이 축선을 덮음 | z-index 불필요(문서 순서로 해결). 셀 span 들 변경 없음 |
| 15 | 타임라인 ↔ 작성자 통계 구분선 제거 | `:995` 작성자 통계 section `mx-5 mt-8 border-t border-[#EDF2F4] pt-7` | **`border-t border-[#EDF2F4]` 제거** (mt-8·pt-7 간격 유지) | 이 카드 내 유일한 가로 구분선 — 오식별 위험 없음 |
| 16 | 저장/공유 바 아이콘 #A9AEBB·세로선 #D4D9E2 | 텍스트는 R4 에서 인라인 #A9AEBB 적용됨(:352,363). **북마크**(BookmarkGlyph :354)는 currentColor 라 이미 #A9AEBB ✓. **공유 아이콘**(:364 `<IconShare size={18}/>`)은 기본 stroke **#7F838D**(icons/index.tsx:579) — 이것이 잔여분. 세로선 `:357` `bg-[#E5EAEE]` | IconShare 에 `stroke="#A9AEBB"` 명시 · 세로선 **#D4D9E2** | 저장됨(active) 상태의 파란색(:352 var(--accent-blue))·훅 단일성 불변. 히어로 아이콘(흰색)은 무관 |
| 17 | 후기 유도 카드 글씨 10% 확대 | 굵은 글씨 `:1166` `text-[18px]` · 작은 글씨 `:1171` `text-[13.5px]` | **20px**(19.8) · **15px**(14.85) | 줄바꿈(`<br/>`) 유지 — 390px 에서 2줄 유지 렌더 확인 |
| 18 | "전문의 Q&A 보러가기" 텍스트 #ffffff | `:1226-1231` Link 에 `text-white` 클래스 — **1번과 동일한 무계층 `a{color:inherit}` 함정**으로 실렌더 어두운 색 | `text-white` → **인라인 `style={{ color:"#fff" }}`** | bg var(--primary)(R4 C-17)·높이 41px 불변 |
| 19 | 다른 시술 순위 radial-gradient 반전 스타일 | `:152-158` `RANK_SETS`(파스텔 bg + 원색 num) · 행 `:1248-1274` — bg 솔리드 파스텔·넘버링/보조/세로선/chevron 원색·시술명 #3A3C41 semibold·`rounded-[24px]`(:1254) | **전면 반전**: RANK_SETS → bg 만: 초록 `radial-gradient(107.17% 766.8% at 129.3% 324.93%, #B4E4DF 0%, #029688 100%)` / 파랑 동일 지오메트리 `#B9DDFC→#1E88E5` / 핑크 `#FBCFDE→#E57B9F` (인덱스 [초록,파랑,분홍,분홍,초록]·6위+ 순환 유지). 텍스트: 넘버링(:1258) `rgba(255,255,255,.65)` · 시술명(:1264) **#FFFFFF font-bold** · 보조(:1265) `rgba(255,255,255,.75)` · 세로선 `rgba(255,255,255,.4)` · chevron(:1269) `rgba(255,255,255,.7)`. 라운드 **rounded-full**(h-58 캡슐) | Figma 추출 지오메트리가 부정확할 수 있음(§4) — 판정 기준: **우하단에서 밝은 기운**. 흰 알파 4종은 첨부 이미지 대조로 ±0.05 조정. `${set.num}CC/4D` hex8 소비 코드 제거(RANK_SETS 구조 단순화 — num 필드 폐기) |

### 2.3. 마이페이지 (/my — MyPageView.tsx)

| # | 항목 | 현재 (파일:줄 · 실제값) | 변경 후 | 파급·주의 |
|---|---|---|---|---|
| 20 | 모바일 헤더 승격(타이틀+종+설정) | 셸 헤더=로고+검색+알림+아바타(AppShell 기본), 그 아래 in-content 타이틀 바(MyPageView:339-367 — h1 22px + 벨 + 설정) | **구조 변경 — §3.2** AppShell `titleHeader` prop 신설. 모바일(<900px) 헤더 = "마이페이지"(19px/800) + 벨(셸 headerActions 재사용 — 미읽음 배지 승계) + 설정 아이콘. 검색 아이콘·아바타 숨김. in-content 타이틀 바는 `hidden min-[900px]:flex`(+모바일용 sr-only h1). 데스크탑 무영향 | in-content 벨(:351-357)엔 배지가 없었음 → 승격으로 배지 생김(개선). hide-on-scroll 은 현행 유지(헤더 전체 동작) |
| 21 | 배경 #F5FBFF | §2.0 참조 (app.module.css:126-129) | — | — |
| 22 | "내 피부 정보" 접힘 하단 여백 1/3 축소 | 카드 `padding: 24`(:370) — 접힘 버튼(:509-532, `padding:"14px 0 0"`) 아래 여백 = 카드 pb **24px** | 접힘 상태에서만 `padding: "24px 24px 16px"` (24×⅔=16, skinOpen 조건부) | 펼침 상태 pb 24 유지(지시가 접힘 한정). grid-rows 트랜지션 중 패딩 점프 미세 — 렌더 확인 |
| 23 | 정보 칩을 닉네임 아래로 | 칩 3종(:402-408)이 프로필 Link 행(:371-399) **바깥** 전폭(아바타 아래까지 깔림), marginTop 12 | 칩 div 를 Link 내부 이름 칼럼(:382-395, `flex:1;minWidth:0`) 안 displayName 아래로 이동, marginTop **6~8** | 행 높이 증가(아바타 56px < 이름+칩) — chevron(:396)은 `alignItems:center` 로 세로 중앙 유지. 칩 wrap 시 레이아웃 렌더 확인 |
| 24 | 정보 칩 사각 라운드 | `Chip`(:144-161) `borderRadius: 999`(:155) — 완전 pill | Chip 에 `radius?: number`(기본 999) prop 추가, **정보 칩 3종만 `radius 8`** | ⚠ 적용 범위 모호(§4): 펼침부 칩(피부고민·관심시술·받은시술)은 pill 유지가 기본안. r 8 vs 10 은 스크린샷 렌더 대조로 확정 |
| 25 | 나의 관심 하트·북마크 아이콘 교체 | 신규 파일 판독 완료: `전달용/02 Icon/마이페이지/heart.svg`=**경로가 현 IconHeart 와 완전 동일, 색만 #F27C97 지정** / `bookmark.svg`=**현 IconBookmark 와 동일, 색 #FED13D**. 현재 나의 관심 행(:575-578)은 NavRow 가 아이콘을 `C.title`(#3A3C41 — :195) 단색으로 렌더 | **신규 아이콘 불필요(글리프 동일)** — NavRow 에 `iconColor?: string` prop 추가, 좋아요·북마크 행 모두 **#383F47**(원장 확정 — 마이페이지 다른 단락 아이콘과 동일 톤) | 요약 통계 카드(③ :539-553)의 하트 #FF6B8A·북마크 #FFC93C(:108-109)는 **현행 유지(원장 확정 — 변경 금지)** |
| 26 | 닉네임 폰트 약간 확대 (< 헤더 타이틀) | `:385` `fontSize: 17` (fontWeight 800) | **18px** — 승격 헤더 타이틀 19px 미만 제약 충족 | 20번 헤더 타이틀 크기(19px)와 연동 — 20번 확정 후 함께 렌더 |

### 2.4. 프로필 "내 정보" (/[handle] — ProfileView.tsx)

| # | 항목 | 현재 (파일:줄 · 실제값) | 변경 후 | 파급·주의 |
|---|---|---|---|---|
| 27 | 배경 #F5FBFF | §2.0 참조 (app.module.css:130-133) | — | — |
| 28 | 통계 폰트 확대 | StatCol(:138-164) — 숫자 `fontSize:17 / 800`(:158) · 라벨 `13.5 / 500`(:161) | 숫자 **20px** · 라벨 **15px** (제안값 — 확대율 미지정, 원장 스크린샷 대조 §4) | baseline 정렬(gap 6) 유지. divider 높이 자동 |
| 29 | 통계 색 | 숫자 `#3A3C41`(:158, C.title) · 라벨 `#A0A8B0`(:161, C.gray) | 숫자 **#676B76** · 라벨 **#9DA1AA** | C 팔레트에 신규 키 추가(statNum/statLabel) — C.title/C.gray 는 타 소비처(헤더·이름) 유지 |
| 30 | 카드 그림자 전부 제거 | 실측: 프로필 카드(:448)·EmptyCard(:640)·댓글 섹션(:598)·AccountSwitcherCard·FollowButton = **그림자 없음(flat ✓)**. 유일한 예외 = **글 목록 PostCard**(ui.tsx:1069 `styles.card`) — `app.module.css:853` `box-shadow: var(--card-shadow)`(=0 6px 24px rgba(53,118,160,.07), :35) | **해당 있음(PostCard 한정)**: app.module.css 에 `.flatCards .card { box-shadow: none; }` 신설, ProfileView `cardList` 래퍼(:387 `styles.feedList`)에 `flatCards` 병기 | `.card` 전역 수정 금지(피드 회귀). OtherProfileMenu 팝오버 그림자(:216)는 카드가 아님 — 유지. /my 는 PostCard 미사용 — 해당 없음 |
| 31 | 헤더 `[< 내정보  수정]` 한 줄 | 셸 backHeader(:410)는 화살표만(AppShell:626-635 — 로고 자리 교체). 타이틀 "내 정보"+우측 "수정"은 본문 행(:415-445) | **구조 변경 — §3.3** backHeader 확장(`title`·`action` 슬롯). 모바일 헤더 = ←+"내 정보"+…+수정(타인은 ←+⋯메뉴). 본문 타이틀 행은 `hidden min-[900px]:flex` + 모바일 sr-only h1 | /reports 상세 등 기존 backHeader 소비처(title/action 미지정)는 100% 현행 동일. 데스크탑 무영향 |
| 32 | 정보 칩 r 값 24번과 동일 | TagChip(:118-135) `borderRadius: 999`(:129) | **8px** (24번 확정값과 동일 상수) | ProfileView 내 TagChip 소비는 정보 칩 3종뿐 — prop 없이 직접 변경 가능 |
| 33 | 필터 칩 가로 스크롤 풀블리드 | 칩 컨테이너(:550) `className={styles.chipRow}` — chipRow(app.module.css:667-673) `padding: 10px 2px 14px`, **`.page` 좌우 18px(:583) 안**에 갇혀 스크롤 영역이 화면 끝까지 가지 않음 | **구조 변경 — §3.4** 신규 modifier `.chipRowBleed`: 모바일(<900px)에서 `margin-inline: -18px`(page 패딩 상쇄) + `::before/::after` 고정 스페이서 12px(+gap 8 = 양끝 20px — 현행 좌변 18+2 와 동일 잉크 위치). ProfileView :550 에 병기 | ≥900px 은 no-op(데스크탑 중앙 칼럼에서 풀블리드 부적절). 유사 칩 행 현황: 리포트 허브 정렬 칩(ReportsIndexView:357-366)·2뎁스 후기 정렬 칩(DetailView:1085-1110) 모두 패딩 내 스크롤(비풀블리드) — **이번 범위는 33번 화면만, 확장 여부는 원장 결정** |

---

## 3. 구조 변경 상세 설계

### 3.1. 캔버스 variant 헤더 동색화 (공통·21·27)

`app.module.css` 의 variant 정의(:122-133) 직후에 1규칙 추가:

```css
/* R5 — canvas variant 페이지는 헤더도 바디와 완전 동색(플랫). 하드코딩 틴트·blur 무력화.
   specificity (0,2,0) 이라 모바일 .header(:209)·데스크탑 .header(:2006) 모두 이김. */
.canvasReport .header, .canvasMy .header, .canvasProfile .header {
  background-color: var(--tt-canvas-top);
  background-image: none;
  backdrop-filter: none;
}
```

- 헤더가 불투명 단색이므로 아래 운영 TopNav 차폐(기존 목적) 유지.
- 데스크탑 border-bottom(rgba white, :2011)은 유지(시각 미미) — 렌더 후 거슬리면 variant 스코프로 제거.
- 검증: /reports·/my·/[handle] 에서 헤더·상태바 필러·바디가 한 색 / 피드·투데이 등 비-variant 는 현행 그라데이션 그대로.

### 3.2. 항목 20 — AppShell `titleHeader` (마이페이지 모바일 헤더 승격)

**prop**: `titleHeader?: { title: string; actions?: ReactNode }` (backHeader·back 과 화면당 하나만).

**AppShell 변경(최소)**:
1. 비검색 모드 headerInner 의 로고 분기(:626-645)를 3분기로: `backHeader` → 뒤로가기 / `titleHeader` → `<div className={styles.titleHeaderText}>{title}</div>` / 기본 → 모바일 로고.
2. 모바일 검색 아이콘(:792-805)은 `titleHeader` 지정 시 미렌더(JS 조건 — 아이콘 자체가 모바일 전용이라 데스크탑 무영향).
3. headerActions(:486-525)의 벨은 그대로(미읽음 배지 승계). 아바타 GuardedLink 에 신규 클래스 `styles.avatarBtn` 부착(무스타일), `titleHeader` 시 headerInner 에 `styles.headerInnerTitle` 병기.
4. `{titleHeader?.actions}` 를 headerActions 바로 뒤에 렌더(래퍼 `styles.titleHeaderAction`).

**CSS(신규 4클래스)**:
```css
.titleHeaderText { font-size: 19px; font-weight: 800; color: #3a3c41; }
.titleHeaderAction { display: inline-flex; }
@media (min-width: 900px) {
  .titleHeaderText, .titleHeaderAction { display: none; } /* 데스크탑=로고+GNB 현행 */
}
@media (max-width: 899px) {
  .headerInnerTitle .avatarBtn { display: none; } /* 아바타 숨김(마이 안에서 불필요) */
}
```
행 높이 결정자는 여전히 .iconBtn 38px → `.topStackHidden -62px` 전제 유지.

**MyPageView 변경**:
- `<AppShell active="마이" canvas="my" titleHeader={{ title:"마이페이지", actions: <Link href="/my/settings" aria-label="설정" className={…iconBtn 톤}><IconSettings size={24}/></Link> }} …>`
- in-content 타이틀 바(:339-367)에 `className="hidden min-[900px]:flex"` (inline style 병행) + 모바일 헤딩 계층 보존용 `<h1 className="sr-only min-[900px]:hidden">마이페이지</h1>` 추가(헤더 타이틀은 div — h1 중복 방지).

### 3.3. 항목 31 — backHeader 타이틀+액션 확장

**prop 확장**: `backHeader?: { fallbackHref?: string; title?: string; action?: ReactNode }` — 기존 소비처(/reports 상세: fallbackHref 만)는 무변화.

**AppShell**: `.backHeaderBtn`(:630-635) 직후 `{backHeader.title && <div className={styles.backHeaderTitle}>{backHeader.title}</div>}`, headerActions 뒤 `{backHeader.action && <div className={styles.backHeaderAction}>{backHeader.action}</div>}`. title 또는 action 지정 시 headerInner 에 `styles.headerInnerBack` 병기.

**CSS**:
```css
.backHeaderTitle { font-size: 18px; font-weight: 800; color: #3a3c41; min-width: 0;
  overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
@media (min-width: 900px) { .backHeaderTitle, .backHeaderAction { display: none; } }
@media (max-width: 899px) {
  /* 스크린샷 [< 내정보    수정] — 우측은 액션만: 검색·벨·아바타 숨김 */
  .headerInnerBack .iconBtnSearch, .headerInnerBack .bellWrap, .headerInnerBack .avatarBtn { display: none; }
}
```

**ProfileView**:
- `backHeader={{ fallbackHref: isOwner ? "/my" : "/", title: isOwner ? "내 정보" : undefined, action: isOwner ? <Link href="/my/settings" …>수정</Link> : <OtherProfileMenu/> }}` (수정 링크 스타일 = 현 :430-441 의 15px/600/#A0A8B0 그대로 이전).
- 본문 타이틀 행(:415-445)은 `hidden min-[900px]:flex` + 모바일용 `<h1 className="sr-only min-[900px]:hidden">내 정보</h1>`(본인만). 타인은 타이틀 없음 — 행 자체가 데스크탑 전용이 됨.

### 3.4. 항목 33 — 필터 칩 풀블리드 (`.chipRowBleed`)

공용 `.chipRow`(AppShell 칩바도 소비)는 건드리지 않고 modifier 신설:

```css
/* R5-33 — 프로필 필터 칩: 모바일에서 .page 좌우 18px 를 상쇄해 스크롤 영역을 화면 전폭으로.
   양끝 잉크 여백은 가상 스페이서(12px + gap 8 = 20px — 현행 좌변 18+2 와 동일)로 보장
   (스크롤 컨테이너의 trailing padding 은 일부 브라우저에서 무시되므로 ::after 스페이서 채택). */
@media (max-width: 899px) {
  .chipRowBleed { margin-left: -18px; margin-right: -18px; }
  .chipRowBleed::before, .chipRowBleed::after { content: ""; flex: 0 0 12px; }
}
```

ProfileView :550 → `className={`${styles.chipRow} ${styles.chipRowBleed}`}`. 기존 패턴 근거: 2뎁스 하단 패널의 `-mx-[18px]` 풀블리드(DetailView:1075)와 동일한 "페이지 패딩 음수 상쇄" 방식.

---

## 4. 자산 대기 · 모호 항목 (원장 확정 완료 — 2026-07-09)

| # | 사안 | **원장 확정** |
|---|---|---|
| 7 | **TT symbol.svg 미수령** — Mac 경로만 전달, `전달용` 전수 확인 결과 부재 | **자산 대기 확정 — 이번 구현 제외.** §2.2-7 설계(삽입 지점·방식)는 유지하고 코드 주석 1줄로 대기 표기. 파일 수령 즉시 후속 커밋(다른 항목 배포를 막지 않음) |
| 6 | "간격 1/3 축소" 해석 — ⅔ 로(32→21) vs ⅓ 로(32→11) | **현재 간격의 2/3 로 축소(1/3 을 줄임) 확정** → **mt-5(20px)** |
| 19 | radial-gradient 지오메트리(Figma 추출 — 부정확 가능) + 흰색 계열 알파 4종 + 캡슐 라운드 | **지정 radial 3종 그대로 적용 후, 첨부 이미지 인상(진한 원색 배경 + 흰색 계열 텍스트·넘버링 반투명·세로선 흰 반투명·chevron 흰)과 대조해 최대한 비슷하게 미세 조정** 확정. Cinzel 넘버링·행 구조는 R4 유지. 판정 기준: 우하단 밝은 기운 |
| 24·32 | r 값 8 vs 10 / 적용 범위(정보 칩 3종만 vs 카드 내 전체 칩) | **r=8px · 정보 칩 3종(30대·각진형·건성)만** 확정(펼침부 칩 pill 유지) |
| 25 | 새 아이콘 색(#F27C97/#FED13D)을 요약 통계 카드(하트 #FF6B8A/북마크 #FFC93C)에도 통일할지 | **나의 관심 행 하트·북마크는 새 글리프(현 IconHeart/IconBookmark 와 동일 판독)로 교체하되 색 #383F47**(마이페이지 다른 단락 아이콘과 동일 톤) 확정. **요약 통계 카드 아이콘 색은 현행 유지(변경 금지)** — 기본안 '통일'은 기각 |
| 28 | 통계 확대율 미지정 | **숫자 20px · 라벨 15px** 확정 (+29번 색 숫자 #676B76·라벨 #9DA1AA) |
| 공통 | 헤더 동색화를 데스크탑(≥900px) 헤더에도 적용할지 | **모바일 + 데스크탑(≥900px) 모두 적용** 확정 |
| 33 | 풀블리드 패턴을 리포트 허브·2뎁스 후기 정렬 칩에도 확장할지 | **내 정보(ProfileView) 필터 칩만** 확정 — 타 칩 행 확장 금지 |

---

## 5. 구현 순서 · 검증 체크리스트

**구현 순서** (단일 에이전트 순차):

1. `app.module.css` — 캔버스 2색 변경(21·27) + 헤더 동색화(공통) + `.chipRowBleed`(33) + `.flatCards .card`(30) + titleHeader/backHeader 신규 클래스(20·31)
2. `AppShell.tsx` — `titleHeader` 신설 + `backHeader` title/action 확장 (기존 소비처 무변화 확인)
3. R5-1 리포트 1뎁스 — ReportSummaryBox(2·3·3'·5) + ReportsIndexCard(1·4·6)
4. R5-2 리포트 2뎁스 — ReportsDetailView(7 제외 전부: 8~19)
5. R5-3 마이페이지 — MyPageView(20·22~26)
6. R5-4 프로필 — ProfileView(28~33)
7. `npx tsc --noEmit` + `npm run build` → code-reviewer 검수(치명 0) → commit/push → CHANGELOG·(구조 변경분) ARCHITECTURE 갱신

**렌더 검증** (390px + 데스크탑 ≥900px, dev http://localhost:4000 — SW 해제 후):

- [ ] 헤더·상태바 필러·바디 동색: /reports(1·2뎁스)·/my·/[handle] 각 #F5FBFF / 비-variant(피드·투데이·내노트·글쓰기·admin) 현행 유지
- [ ] 1뎁스: CTA 2버튼 글자색(흰/짙은 회색 — 인라인 style 적용 확인) · 통증/만족도 20% 확대 후 막대 하단 잉크 정렬 · 96% SemiBold · 96%↔라벨 간격
- [ ] **/topics 닫힌 글상자** — SummaryBox 동반 변경 반영·비깨짐(불변 조건 1)
- [ ] 2뎁스: 통증 게이지 마커 우측이 #EDF2F4 트랙(오버레이) · 다운타임 채움 애니메이션 무회귀 · 타임라인 원이 축선 위 · 순위 리스트 radial+흰 텍스트(모바일 Safari 포함) · Q&A 버튼 흰 글자 · 저장/공유 아이콘 #A9AEBB(저장됨 파란색 토글 무회귀)
- [ ] 마이: 모바일 헤더 승격(배지 동작·hide-on-scroll·탭바 무회귀) · 데스크탑 in-content 타이틀 현행 · 칩 위치/라운드 · 접힘 하단 여백 · 새 아이콘 색
- [ ] 프로필: 모바일 `[< 내정보  수정]` / 타인 `[<  ⋯]` · 데스크탑 뒤로 행+본문 타이틀 현행 · 통계 크기/색 · PostCard 그림자 제거(피드의 PostCard 는 그림자 유지 확인) · 필터 칩 풀블리드(첫/끝 여백 20px·스크롤 시 가장자리 통과·≥900 무영향)
- [ ] `.topStackHidden` 스크롤 숨김 어긋남 없음(헤더 높이 불변 전제)
- [ ] backHeader 기존 소비처(/reports 상세) 헤더 100% 현행 동일

**회귀 요주의**: `:where(.root)` reset 은 무계층 — 새 글자색은 전부 인라인 style / AnchorEngagement 훅 인스턴스 단일성(16번은 색만 변경) / ReportSummaryBox prop 계약 불변.
