# 260709 리포트 디자인 정밀 보정 계획 (R4)

> 명세 SSOT: `전달용/260708 UI개편/[피부텐텐] UXUI 디자인 개선 및 기능 오류 피드백 (2).pdf` + 시안 PNG 4장(같은 폴더).
> 시안 실측 기준: PNG 1179px 폭 = **390pt 캔버스 @3.023x** 환산(검토 1 정정 — 카드 348·마진 21 이 390 기준 정합).
> 선행 트랙: 260708 UI 개편 본편 + R2 + R3 (`260708 UI 개편 진행 로그.md`). 본 R4 는 그 후속 정밀 보정.
> 버전: **v2 (확정본)** — v1 을 독립 검토 2인(시안 충실도 / 구현 타당성) + 인물 그리드 데스크탑 자문으로 교차 검증, 전 지적 반영(§9).

---

## 0. 원장 확정 사항 (2026-07-09)

1. PDF p5 수정사항 9번("후기 바로위에 있는 박스에" — 원문 끊김) → **무시**.
2. Cinzel: 구글 폰트 사용 승인. next/font 는 turbopack 이슈로 프로젝트 전체가 우회 중(layout.tsx:28-30)이므로 **self-host @font-face**(§5.5). 서브셋 woff2(3.2KB, 가변 1파일)는 디렉터가 이미 취득·검증 완료(세션 scratchpad `cinzel-subset.woff2`).
3. 하단 저장/공유 바: **화면 고정(fixed) 아님 — 페이지 본문 속 일반 요소**로, 스크롤 시 콘텐츠와 함께 지나감(원장 재확인).
4. 2뎁스 인물 그리드가 데스크탑에서 옆으로 늘어나는 문제(원장 캡처 제보) → **그리드 폭 상한** 방식 채택(§5.2 C-7).

## 1. 범위

| 화면 | 파일 | 작업 |
|---|---|---|
| 1뎁스 접힘 | `src/components/report/ReportSummaryBox.tsx` (+chevron 은 `ReportsIndexCard.tsx`) | 폰트 크기·굵기·색·간격 교정 |
| 1뎁스 펼침 | `src/app/reports/ReportsIndexCard.tsx` + `globals.css` 토큰 | 통증 게이지 채움 방식 변경·마커 스펙·굵기·간격 |
| 2뎁스 상세 | `src/app/reports/[procedure]/ReportsDetailView.tsx` (+`procedure-theme.ts`, `globals.css`, `icons`) | 카드 분리·하단 바 인라인화·Cinzel·그리드 17열+폭상한·타임라인·순위 리스트 교체·굵기 일괄 SemiBold·회색 치환 |

**비변경**: ReportsReviewCard(후기 카드 — PDF ⑧ "후기 부분 제외" 명시), SEO 셸(JSON-LD·canonical·308), 데이터 배선(RPC·집계), ReportShareButtons(데스크탑 사이드바).

**카피 불변**: painPhrase 등 문구 엔진 유지. 시안 속 문구("많이 아픈 정도예요"·"평균 약 2주")는 예시 상태 표기로 보며, 프로즈 카피는 R2 라운드에서 원장이 "확정 카피 유지"로 판정한 사안(진행 로그 2026-07-09 R2) — 재변경하지 않음.

## 2. 불변 조건 (회귀 금지)

1. **ReportSummaryBox 는 /topics 닫힌 글상자와 공유 SSOT** — 변경이 `topics/[tag]/TopicTagView.tsx:91` 에 그대로 반영(의도된 동반 변경). 완료 후 두 화면 모두 검증.
2. **저장·공유 배선 보존** — AnchorEngagement 훅 인스턴스 단일 유지(§5.1 C-2 의 "인라인 슬롯 포털" 방식). 상태 이원화 금지.
3. 접근성 유지: aria-expanded·aria-hidden+텍스트 병기·포커스 링 패턴 그대로.
4. 애니메이션(펼침 grid-rows, 효과 막대 0→값 채움) 유지.
5. `#8A939B→#7F838D` 치환은 **리포트 도메인 파일 한정**(전역 sed 금지). 단 이번에 명시된 줄만(§3 A-15 / §4 B-19 / §5.3 C-23).
6. 렌더 검증 시 헤드리스 프리뷰 rAF 미발화 함정 주의(막대 애니메이션 미발화를 회귀로 오판 금지).
7. **오버레이 검수 주의(검토 1-9)**: 효과 막대 채움색·순위 리스트 스타일은 **시안 PNG 와 PDF 명문값이 서로 다름 — PDF 우선 확정**. 오버레이 대조에서 "색 불일치"로 오판 금지.

## 3. R4-1 — 1뎁스 접힘 (ReportSummaryBox + chevron)

시안: `1d-리포트-접힘.png`. 카드 348×~201·마진 21·카드 간 16(현행 유지), 패딩 p-6 유지.

| ID | 항목 | 현재 (파일:줄) | 변경 후 |
|---|---|---|---|
| A-2 | 시술명 | `text-[18px] font-extrabold` (SummaryBox:76) | **21px / font-bold(700)** / 색 theme.color 유지 |
| A-3 | 카테고리 칩 | `text-[11px] font-bold px-2.5 py-[3px]` (:83-84) | **13px / font-medium(500)** / py 4px(칩 높이 ~23). 검토 1: 시안 잉크는 14px 근사 — 렌더 대조 후 13↔14 확정. 색 유지(#CDF0EC 앵커 ✓) |
| A-4 | 재시술 % 큰 숫자 | `text-[46px] font-extrabold` + % 22px (:93-96) | **50px / font-bold(700)**, % 비례 유지(≈24px). 시술명과 잉크갭 21 — 렌더 대조 미세조정 |
| A-5 | "재시술의향" 라벨 | `text-[13px] font-bold text-[#8A939B]` (:103) | **14px / font-semibold(600) / #3A3C41** |
| A-6 | "N건의 경험" | `text-[11.5px] text-[#8A939B]` (:104) | **14px / font-normal / #7F838D** (검토 1 정정: 잉크가 A-5 와 동일 → 13→14px) |
| A-8 | 진행 막대 | `h-[9px]` (:109) | **h 8px**. 채움 #B4E4DF→theme.color·트랙 #fff 유지. 좌우 블록 간격 **gap-5(20) 현행 유지** (검토 1 정정: 실측 20.2 — v1 의 gap-6 폐기) |
| A-9 | "통증"/"만족도" 라벨 | `text-[11px] font-semibold text-[#8A939B]` + mb-1 (:119,134) | **13px / font-normal / #7F838D** / mb 5~6px. 좌측 라벨행과 같은 잉크 라인 |
| A-10 | 수치 5.0 / 4.9 | `text-[18px] font-extrabold` (:125,139) | **22px / font-semibold(600) / #3A3C41**. `items-end` 유지 — **수치 하단 = 막대 하단**(시안 Δ1.65) 렌더 검증 |
| A-11/12 | 번개·별 아이콘 | size 14 (:124,138) | **size 16** (색 유지). 통증↔만족도 그룹 간격 **gap-4(16)** (검토 1 정정: 실측 16.5 — v1 의 14 폐기) |
| A-13 | 헤드라인 문장 | `mt-4 text-[13px] text-[#8A939B]` (:149) | **14px / #7F838D** (검토 1 정정: 13.5→14). mt-4 유지 후 렌더 대조 |
| A-14 | 펼침 화살표 | `stroke="#9FB3AC"` (IndexCard:135) | **#B5DBD6** (크기·위치 현행 유지) |
| A-15 | 회색 공통 | #8A939B (SummaryBox) | **#7F838D — 5곳만: :104,119,130,134,149** (":103 은 A-5 로 #3A3C41" — 검토 1 충돌 해소) |

폰트 굵기 명세 대응(PDF): 시술명 Bold / 칩 Medium / 재시술의향 SemiBold / "N건의 경험"·헤드라인·통증·만족도 라벨 Regular / 수치 SemiBold.

## 4. R4-2 — 1뎁스 펼침 (ReportsIndexCard + globals.css)

시안: `1d-리포트-펼침.png`. 펼침부 패딩 **px-5(20)**(접힘 24 와 의도적 상이), pb ~24.

| ID | 항목 | 현재 (파일:줄) | 변경 후 |
|---|---|---|---|
| B-1 | 통증 그라데이션 토큰 | `--pain-grad-1~3` 3스톱 (globals.css:103-105) | **4스톱 #FFDD77→#FFB46D→#FF7B9F→#FF565B**(`--pain-grad-4` 신설, :102 "스톱 3종" 주석 갱신). 소비처는 IndexCard:42 유일(재확인 완료) → 2뎁스 PAIN_STOPS(DetailView:88-101)도 **토큰 소비로 통합**. 2뎁스 `painMarkColor`(값 버킷 색)는 **폐기** — 통증 마커 색은 1·2뎁스 모두 **#F06258 고정**(시안 부합·SSOT 단순화). 양쪽 "의도적 이원화" 주석 삭제 |
| B-2 | 게이지 채움 | 트랙 전체 그라데이션 (:176-179) | **오버레이 방식**(검토 2 대안 채택 — background-size 재스케일 함정 회피): 트랙 div 에 4스톱 그라데이션 풀폭 + `overflow-hidden rounded`, 그 위 우측 절대배치 `bg-[var(--gauge-track)]` span 을 `width: (100 - 채움%)%` 로 덮음. 마커는 그 위 z. 0% 가드 불요·그라데이션 스케일 불변 |
| B-3~5 | 마커 | 22px / border-2 / 그림자 없음 (:182) | **원 20px / 외곽선 1px #F06258 / box-shadow 0 3px 5px rgba(0,0,0,0.1)** / IconPain size 11 유지(20px 원 기준 번개 가로 8.5px 충족) |
| B-6 | 마커 위치 | painPos 6.25~93.75% (:46-49) | painPos 를 **0~100% 매핑**으로 바꾸고 `left: clamp(10px, {pos}%, calc(100% - 10px))` (검토 2: % 단위 clamp 불가 — px 혼합 CSS clamp). **2뎁스 통증(:772)·다운타임(:803) 마커도 동일 매핑·clamp 로 통일**(같은 값 = 같은 위치) |
| B-7 | "통증 평균 N점" | font-bold (:169) | **font-semibold**. 제목→바 간격 mt-4→**mt-6(24)** (실측 잉크갭 24) |
| B-8 | 척도 라벨 5종 | `text-[11px] #8A939B` (:192) | **13px / #7F838D** (검토 1 정정: A-9 와 동일 잉크 → 12.5→13 통일). 바와 간격 mt-2→**mt-3(12)** |
| B-9 | "무엇이 좋아졌나요?" | font-bold (:205) | **font-semibold** |
| B-10 | 효과 항목명 | `text-[13px] font-semibold` + `w-[56px]` + gap-3 (:210-211) | **14px / font-normal** / **`w-[37px]` + gap-3(12) 유지** (검토 1 정정: 실측 49.3 은 "라벨+간격" 전체 — 37+12=49. 긴 라벨 잘림 시 truncate 유지) |
| B-11 | 효과 % | `text-[13px] font-bold #0E8A6E` (:225) | **16px / font-semibold / #029688** |
| B-13 | 효과 막대 | h-[9px] · 행 gap-3 (:208,214) | **h 10px** · **행 gap-2(8)** (검토 1 정정: 라벨 14px 행박스 기준 피치 29.5 정합 — v1 의 gap-3.5 폐기) |
| B-14 | 효과 시점 문구 | Regular + 강조어 bold+색 (:236-242) | **문장 전체 semibold #3A3C41**, 강조어는 **색 #029688 만**(굵기 추가 없음) |
| B-15/16 | CTA 2버튼 | font-bold / py-3.5 / gap-2.5 (:246-268) | **font-medium / 높이 42px / gap-4(16)**. 색·라운드 14px 유지 |
| B-18 | 블록 간격 | 일괄 gap-6(24) (:164) | 실측 32/31/20.5 → **gap 해체, 개별 mt: 통증→효과 32 · 효과→시점 31(≈mt-8) · 시점→CTA 20(mt-5)**. 펼침부 px-5(20)·pb-6 |
| B-19 | 보조문구 회색 | painPhrase 스팬 #8A939B (:172) | **#7F838D** (검토 1: v1 누락분 명시) |

## 5. R4-3 — 2뎁스 상세 (ReportsDetailView + procedure-theme + globals.css + icons)

시안: `2d-리포트-1.png`, `2d-리포트-2.png`.

### 5.1. 구조 변경

| ID | 항목 | 현재 | 변경 후 |
|---|---|---|---|
| C-1 | 카드 분리 | 히어로+흰 통계 한 카드(:602-994) | **형제 카드 2장, 간격 16px(mt-4)**. 히어로 section 자체가 rounded+overflow-hidden 보유(:607 — 워터마크 클립 유지, 검토 2 확인). 흰 카드에 래퍼 신설 |
| C-2 | 저장/공유 바 | createPortal(document.body) fixed z-105 bottom 76px (:314-347) | **인라인 슬롯 포털**(검토 2 [치명] 대안 채택): 포털은 유지하되 target 을 body → **본문 인라인 슬롯 div** 로 교체. 슬롯 위치 = **흰 통계 카드(:994 닫힘)와 #F5FBFF 리뷰 패널(:998) 사이**, 흰 카드 아래 19px. fixed·z-105·bottom·safe-area·boxShadow·`portalReady` rAF·`useSoftKeyboardOpen` 숨김 **제거**(일반 흐름 스타일만). 텍스트 **#A9AEBB**·배경 칩 없음·항목 간 세로 구분선. 모바일 전용(`min-[900px]:hidden`) 유지. 히어로 아이콘과 훅 인스턴스 단일 유지. WriteFab.tsx:28-29 낡은 주석 갱신(동작 무변) |
| C-3 | "PIBUTENTEN REPORT" 표기 | F5FBFF 패널 맨 끝 13px #C3CFD9 (:1196-1201) | **흰 통계 카드 맨 아래로 이동**(카드 바닥 위 ~28px) — **demographics 조건부 밖, 카드 직속 마지막 자식**(검토 2: 통계 없는 시술에서도 렌더). 스타일: **~10.5px / semibold / #D4D9E2 / 광폭 트래킹**(검토 1 실측). 기존 패널 끝 표기 **삭제**(중복 금지) |

### 5.2. 히어로

| ID | 항목 | 현재 | 변경 후 |
|---|---|---|---|
| C-4 | "피부텐텐 리포트" 스크립트체 | 텍스트 (:622) | **현행 유지** — asset 미수령(원장 보고 항목) |
| C-5 | tt: 워터마크 | IconBrandTT (:614-618) | **유지** (시안 동등) |
| C-6 | 100% 숫자 | Pretendard 95px extrabold + tracking/tnum (:646-649) | **Cinzel(§5.5) ≈96~100px Regular** — 잉크 69.2 기준 재캘리브레이션. tracking·font-feature 제거. % 첨자 비례 유지. **카운트업(useCountUp) 숫자폭 지터**는 렌더 확인 후 필요 시 숫자 span 고정폭 완충(검토 2-8b) |
| C-7 | 사람 그리드 | 20열×3 (:509,655-677) | **17열×3(=51셀)**, 머리 φ7.9 비율(셀 ~11.4·열 gap ~5.8·행 피치 20.5) + **그리드 블록 `max-width` 상한 ≈283px**(17열 조판 실폭으로 확정) — 데스크탑(콘텐츠 최대 626px)에서 셀이 옆으로 2.9배 늘어나는 왜곡 제거(자문 채택: 폭 상한·좌측 정렬, <900px 는 no-op). IconPersonGrid 글리프 재작업 불요 |
| C-8 | 히어로 시술명 | 37px extrabold (:624) | **40px / font-bold(700)** — 세로 리듬(칩→재시술의향 34.5 등) 재검증 |
| C-9 | 태그 칩 | rgba(0,88,71,.40) 오버레이 (:126-128,634) | **솔리드화**(검토 2-5 규칙 확정): 초록 앵커 **#029688→#13887B**, 타 카테고리는 **hue=카테고리색·S/L=#13887B 목표**로 procedure-theme 의 hexToHsl/hslToHex 파생 재사용. null 카테고리는 고정 hex 폴백 명기. `person: #168275` 존치. 부수: :451 color-mix iOS15 가드 제거 가능 |
| C-10 | 헤드라인 | 전체 font-bold (:688) | **디폴트 Medium / 강조 스팬만 Bold** (색 #FFF8D1/#FFFFFF 유지) |
| C-11 | 통계 캡션 | white/65 (:680) | **white/60** |
| C-12 | 배경 그라데이션 | 130deg 평탄 21% → gradEnd #90D5CE (:609, theme:97,105) | 종점 **#92D5CE**. 각도는 **130deg 유지 + 평탄 구간 연장(21%→~35%) 을 1순위로 렌더 비교**(검토 1-8 정정: 시안 하단 좌우가 다름 = 대각 성분 존재, 180deg 단정 폐기). 판정 기준: 하단 좌우 색 차 재현 |

### 5.3. 흰 통계 카드

| ID | 항목 | 현재 | 변경 후 |
|---|---|---|---|
| C-13 | 굵기 일괄 | extrabold/bold 다수 (:209-211,725,767,794,823,841,852,887-888,935,1087,1124,1180) | **후기 카드 제외 전부 SemiBold**. 효과 % 색 #1A1A1A→**#3A3C41** |
| C-14 | 다운타임 바 | 채움·마커 #6EC1F0 (:112,797-812) | **채움 #D9D9D9→#9DA1AA / 트랙 #EDF2F4 / 마커 #9DA1AA**. 채움은 **B-2 와 동일한 오버레이 방식**(전폭 기준 그라데이션 — dtPct<100% 재스케일 금지, 검토 1-12) |
| — | 통증·다운타임 마커 | 24px / border-2 (:772,:803) | **20px / 1px / 그림자**(B 스펙 통일). 위치 매핑도 B-6 과 통일 |
| C-15 | 타임라인 | 막대 40px·개별 그라데이션 (:894-901)·원 18px #1A9DE8 (:915) | **막대 폭 34px**, 채움 `linear-gradient(180deg,#2994DB 0%,#9AE4FF 83%,#FFFFFF 100%)` 를 **`backgroundSize:"100% 126px"`(=차트 최대 높이 상수 18+108, :881) + `backgroundPosition:"bottom"`** 으로 고정(검토 2-4 — height transition 양립). 상수는 그라데이션과 한 곳에 묶어 동기화 주석. 축 원 φ19 링 **#2994DB**, 최다 수치 **#2994DB ~17px**. 짧은 막대(밝은 구간만 노출)의 시인성은 렌더 대조 확인 항목 |
| C-16 | "n건의 후기 더보기"·"6~10위 보기" | #8A939B (:1065,1139) | **#A9AEBB** |
| C-17 | 전문의 Q&A 버튼 | var(--accent-blue) (:1147) | **`var(--primary)`**(=#4CBFF2, PDF 지정값과 동일 — 검토 2-11 채택: 브랜드색 연동) / 텍스트 #fff / 높이 ~41px |
| C-21 | 후기 정렬 칩 | sticky (:1006-1034) | **유지** |
| C-23 | 회색 치환 | #8A939B 리터럴 다수(:728,744,768,783,795,815,835,859,923,937,952 등) | **#7F838D 일괄**(검토 1-6: v1 누락 명시 — 이 파일 내 시안 보조텍스트 대응 줄 전수. 단 C-16 두 줄은 #A9AEBB) |

### 5.4. "'○○' 효과가 좋았던 다른 시술" 순위 리스트 전면 교체 (C-18)

현재(원형 배지+카테고리 chip, :1156-1193) → PDF p5-6 신 스타일(시안 PNG 는 구 스타일 — PDF 우선, §2-7):

- 행: **Cinzel Medium 넘버링 01~05** + 시술명(Pretendard SemiBold **#3A3C41** — 검토 1-10) + 세로 구분선 + "후기 N개 | ○○ 효과 N%" + 우측 chevron. 행 높이 ~57.6·행 간 8(실측).
- 색 3세트(인덱스 고정 **[초록, 파랑, 분홍, 분홍, 초록]**, 6위+ 순환):
  - 초록: 배경 #B4E4DF / 넘버링 #029688 / 본문 #029688 80% / 세로선 #029688 30%
  - 파랑: 배경 #BCDAF5 / 넘버링 #1E88E5 / 본문 #1E88E5 80% / 세로선 #1E88E5 30%
  - 분홍: 배경 #FBCFDE / 넘버링 #E57B9F / 본문 #E57B9F 80% / 세로선 #E57B9F 30%

### 5.5. Cinzel 폰트 도입

- 파일: 디렉터 취득본 `cinzel-subset.woff2`(3.2KB, 숫자 10자+%, **가변 1파일이 400·500 커버**) → `public/fonts/cinzel-subset.woff2`. **OFL 라이선스 동봉**: `public/fonts/OFL-Cinzel.txt` (https://raw.githubusercontent.com/NDISCOVER/Cinzel/master/OFL.txt).
- globals.css @font-face 1벌: `font-family:"Cinzel"; font-weight: 400 500; font-display: swap; unicode-range: U+25, U+30-39;` 폴백 `Georgia, serif`. layout.tsx 에 preload `<link>` (Pretendard 패턴).
- 적용: 2뎁스 히어로 100%(C-6) + 순위 넘버링(§5.4) **만**. 타 화면 사용 금지.

## 6. 작업 순서·검증

1. **구현 순서**: globals.css 토큰(B-1)+Cinzel 도입 → R4-1(SummaryBox+chevron) → R4-2(IndexCard) → R4-3(DetailView·theme·icons 소비부). 단일 구현 에이전트 순차.
2. **정적 검증**: `npx tsc --noEmit` (전체 build 는 디렉터가 1회).
3. **렌더 검증**(390px 뷰포트 + 데스크탑 ≥900px): 시안 오버레이 대조 — A-10 수치·막대 하단 정렬 / B-2·C-14 오버레이 채움 / C-15 타임라인 / C-7 그리드 폭 상한(데스크탑 비왜곡). §2-7 오판 주의.
4. **회귀 체크리스트**(검토 2-12 반영): ① /topics 닫힌 글상자 ② 데스크탑 사이드바 ReportShareButtons 저장 상태 동기(모듈 스토어) ③ 앵커 없는 리포트(저장 비노출·URL 공유 폴백 :701-713) ④ 1뎁스 접힘↔펼침 반복 revealed 재애니메이션 ⑤ Cinzel 카운트업 지터 육안 ⑥ 후기 sticky 정렬 칩 스크롤 ⑦ WriteFab /reports 분기 무회귀.
5. **검수 루프**: 독립 시안 대조 검수 1인 + code-reviewer(치명 0) + 디렉터 → 핀셋 수정 반복.
6. commit·push(Vercel) → CHANGELOG·ARCHITECTURE §4.3 갱신 → 원장 보고(스크립트체 asset 미수령 후속, **#A9AEBB 저장/공유 라벨 대비 2.2:1 AA 미달**(시안 지정색 준수 — 접근성 트레이드오프) 고지 포함).

## 7. 결정 지점 처리 내역 (디렉터 채택)

| 항목 | 채택 |
|---|---|
| PDF ⑨ | 무시 (원장 확정) |
| 스크립트체·TT symbol asset 부재 | 현행 유지, 수령 시 후속 |
| 통증 그라데이션 | 4스톱 토큰 + 1·2뎁스 SSOT 통합, 마커 색 #F06258 고정(painMarkColor 폐기) |
| #7F838D 범위 | 명시 줄 한정(A-15 5곳 / B-8·B-19 / C-23) |
| 순위 3색 | [초록,파랑,분홍,분홍,초록] 고정, 6위+ 순환. 스타일은 PDF 우선(시안 PNG 는 구판) |
| PIBUTENTEN REPORT | 흰 카드 이동(조건부 밖) + 기존 표기 삭제 |
| 후기 정렬 칩 | 유지 |
| 2뎁스 마커 | 1뎁스 스펙(20px/1px/그림자)+위치 매핑 통일 |
| 히어로 그라데이션 | 130deg 유지+평탄 연장 1순위 렌더 비교(180deg 단정 폐기) |
| 인물 그리드 데스크탑 | 폭 상한 ≈283px(17열 조판 실폭) 좌측 정렬 |
| Q&A 버튼 | var(--primary) 소비(값 동일) |
| 저장/공유 바 | 인라인 슬롯 포털(훅 단일 유지), 흰 카드↔F5FBFF 패널 사이 |

## 8. 상태

- [x] 토큰·Cinzel 도입
- [x] R4-1 구현
- [x] R4-2 구현
- [x] R4-3 구현 (+범위 추가: 히어로 헤드라인 회전 엔진 연결 — 원장 확정 2026-07-09, C-7 폭 상한 포함)
- [x] tsc/build·렌더 검증·회귀 체크리스트 (Playwright 실브라우저 — SW 해제 후 하이드레이션 0. 검증 중 실버그 1건 발견·수정: `:where(.root) button{color:inherit}` 무계층 규칙이 버튼 글자색 유틸리티를 무시 → 3곳 인라인 style 화)
- [x] 검수 루프(시안 대조 O/△/X 전수 — 미반영 0·△1 수용(B-14 theme.color 유지: 타 카테고리 일관성) / code-reviewer 치명 0·경고 5 중 구조 지적 1 반영 / 디렉터 실렌더 대조)
- [x] commit/push·문서·보고 — 커밋 bc56ccd (2026-07-09, Vercel 자동 배포). CHANGELOG·PRD §4.0/§4.3 동기 갱신.

## 9. 변경 이력

- v1 (2026-07-09): 갭 분석(디자이너 A) 기반 초안.
- v2 (2026-07-09): 독립 검토 2인 + 자문 반영 —
  - 검토 1(시안 충실도): A-15↔A-5 충돌 해소(:103 제외) / A-8 gap-5 유지·A-11/12 gap-4 정정 / A-6·A-13 14px·B-8 13px 정정 / B-10 라벨 37px 산식·B-13 gap-2 정정 / B-19·C-23 회색 치환 명시 추가 / C-2 위치 문구 확정 / C-12 180deg 폐기 / C-3 스타일 실측 명시 / §5.4 시술명 색 / C-14 오버레이 명기 / 캔버스 390pt 정정 / PDF-시안 불일치 각주(§2-7).
  - 검토 2(구현 타당성): C-2 인라인 슬롯 포털 [치명] / B-2·C-14 오버레이 방식 / C-15 backgroundSize 고정 상수 / C-9 hue 파생 규칙 / B-1 painMarkColor 폐기 / B-6 px 혼합 clamp / Cinzel UA·OFL·preload·지터 / C-3 조건부 밖 / #A9AEBB 대비 고지 / 검증 체크리스트 확장.
  - 자문(인물 그리드): C-7 폭 상한 채택.
